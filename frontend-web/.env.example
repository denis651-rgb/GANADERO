# Copiar como frontend-web/.env.local si se necesita cambiar algo. No confirmar el archivo resultante.
# Solo aplica corriendo el frontend suelto (npm run dev); dentro de Electron la URL
# base del backend la inyecta el proceso principal (window.ganadero.apiBaseUrl).
VITE_API_URL=http://localhost:8080
