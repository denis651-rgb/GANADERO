import { useState, type FormEvent } from 'react'
import { Link } from 'react-router-dom'
import { supabase } from '@/auth/supabase'
import { Alert } from '@/shared/components/Alert';import { Button } from '@/shared/components/Button';import { Field } from '@/shared/components/Field'
export function ForgotPasswordPage(){const[email,setEmail]=useState(''),[loading,setLoading]=useState(false),[message,setMessage]=useState<string|null>(null);async function submit(event:FormEvent){event.preventDefault();setLoading(true);try{if(supabase)await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/restablecer-contrasena`});setMessage('Si el correo está registrado, recibirás instrucciones para continuar.')}catch{setMessage('Si el correo está registrado, recibirás instrucciones para continuar.')}finally{setLoading(false)}}return <main className="simple-auth-page"><form className="auth-card" onSubmit={submit}><img src="/logo.svg" alt="GANADERO" width="58" height="58"/><div><p className="eyebrow">Recuperación</p><h1>Restablecer contraseña</h1><p className="muted">Te enviaremos instrucciones si el correo está registrado.</p></div>{message&&<Alert tone="info">{message}</Alert>}<Field label="Correo electrónico"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></Field><Button type="submit" fullWidth loading={loading}>Enviar enlace</Button><Link className="text-link centered" to="/login">Volver al inicio de sesión</Link></form></main>}
