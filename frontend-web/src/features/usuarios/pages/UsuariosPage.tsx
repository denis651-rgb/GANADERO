import{useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{Plus,Power}from'lucide-react';import{listRoles}from'@/features/roles/api';import{listPropiedades}from'@/features/propiedades/api';import{createUsuario,listUsuarios,setUsuarioEstado}from'@/features/usuarios/api';import{Alert}from'@/shared/components/Alert';import{Button}from'@/shared/components/Button';import{Card}from'@/shared/components/Card';import{EmptyState}from'@/shared/components/EmptyState';import{Field}from'@/shared/components/Field';import{LoadingState}from'@/shared/components/LoadingState';import{PageHeader}from'@/shared/components/PageHeader';import{normalizeApiError}from'@/shared/api/errors'
export function UsuariosPage(){const client=useQueryClient();const[showForm,setShowForm]=useState(false);const query=useQuery({queryKey:['usuarios-completos'],queryFn:async()=>{const[users,roles,properties]=await Promise.all([listUsuarios(),listRoles(),listPropiedades()]);return{users,roles,properties}}});const create=useMutation({mutationFn:(form:HTMLFormElement)=>{const d=new FormData(form);return createUsuario({email:String(d.get('email')),nombres:String(d.get('nombres')),apellidos:String(d.get('apellidos')),telefono:String(d.get('telefono')??''),cargo:String(d.get('cargo')??''),accesoTodasPropiedades:d.get('accesoTodasPropiedades')==='on',roles:d.getAll('roles').map(String),propiedades:d.getAll('propiedades').map(String)})},onSuccess:()=>{setShowForm(false);client.invalidateQueries({queryKey:['usuarios-completos']})}});const state=useMutation({mutationFn:({id,action,version}:{id:string;action:'activar'|'bloquear';version:number})=>setUsuarioEstado(id,action,version),onSuccess:()=>client.invalidateQueries({queryKey:['usuarios-completos']})});const error=query.error??create.error??state.error
 return <div className="page-stack"><PageHeader eyebrow="Seguridad" title="Usuarios" description="Invita usuarios y administra su acceso empresarial." actions={<Button onClick={()=>setShowForm(v=>!v)}><Plus size={18}/>Nueva invitación</Button>}/>{error&&<Alert tone="danger">{normalizeApiError(error).message}</Alert>}{create.isSuccess&&<Alert tone="success">Invitación enviada correctamente.</Alert>}{showForm&&<Card><form className="form-grid compact-form" onSubmit={e=>{e.preventDefault();create.mutate(e.currentTarget)}}><Field label="Correo"><input name="email" type="email" required/></Field><Field label="Nombres"><input name="nombres" required/></Field><Field label="Apellidos"><input name="apellidos" required/></Field><Field label="Teléfono"><input name="telefono"/></Field><Field label="Cargo"><input name="cargo" defaultValue="COLABORADOR"/></Field><fieldset><legend>Roles</legend>{query.data?.roles.filter(r=>r.activo).map(r=><label key={r.id}><input type="checkbox" name="roles" value={r.id}/> {r.nombre}</label>)}</fieldset><fieldset><legend>Propiedades autorizadas</legend>{query.data?.properties.filter(p=>p.activo).map(p=><label key={p.id}><input type="checkbox" name="propiedades" value={p.id}/> {p.nombre}</label>)}</fieldset><label className="checkbox-line"><input name="accesoTodasPropiedades" type="checkbox" defaultChecked/> Acceso a todas las propiedades</label><div className="form-actions"><Button type="submit" loading={create.isPending}>Enviar invitación</Button></div></form></Card>}<Card>{query.isPending&&<LoadingState message="Consultando usuarios…"/>}{query.data?.users.length===0&&<EmptyState title="No hay miembros" description="Invita al primer usuario de la empresa."/>}{query.data&&query.data.users.length>0&&<div className="table-wrapper"><table><thead><tr><th>Usuario</th><th>Cargo</th><th>Roles</th><th>Acceso</th><th>Estado</th><th/></tr></thead><tbody>{query.data.users.map(user=><tr key={user.id}><td><strong>{user.nombres} {user.apellidos}</strong></td><td>{user.cargo||'—'}</td><td>{user.roles.map(role=>role.nombre).join(', ')||'Sin roles'}</td><td>{user.accesoTodasPropiedades?'Todas':`${user.propiedadesPermitidas.length} asignadas`}</td><td><span className="status-badge">{user.estado}</span></td><td><Button variant="ghost" loading={state.isPending} onClick={()=>state.mutate({id:user.id,action:user.estado==='ACTIVO'?'bloquear':'activar',version:user.version})}><Power size={16}/>{user.estado==='ACTIVO'?'Bloquear':'Activar'}</Button></td></tr>)}</tbody></table></div>}</Card></div>}
