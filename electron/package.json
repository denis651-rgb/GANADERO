{
  "name": "ganadero-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Ganadero como aplicación de escritorio (Electron + backend Spring Boot embebido)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "dev": "npm run build && npm run package:backend && electron .",
    "dev:all": "concurrently -k -n vite,electron -c green,blue \"npm --prefix ../frontend-web run dev\" \"npm run build && npm run package:backend && wait-on http://localhost:5173 && electron .\"",
    "package:frontend": "cd ../frontend-web && npm run build",
    "package:backend": "node scripts/package-backend.js",
    "package:runtime": "node scripts/build-runtime.js",
    "dist": "npm run build && npm run package:frontend && npm run package:backend && npm run package:runtime && electron-builder",
    "release": "npm run build && npm run package:frontend && npm run package:backend && npm run package:runtime && electron-builder --publish always"
  },
  "dependencies": {
    "adm-zip": "^0.6.1",
    "electron-updater": "^6.8.9",
    "exceljs": "^4.4.0"
  },
  "overrides": {
    "exceljs": {
      "uuid": "^11.1.1"
    }
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^24.10.0",
    "concurrently": "^10.0.5",
    "electron": "^44.0.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wait-on": "^9.1.0"
  }
}
