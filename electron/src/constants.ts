/** URL del servidor de Vite en desarrollo — la usan main.ts y manual-export.ts para no importarse
 * entre sí en círculo (main.ts ya importa exportManualPdf de manual-export.ts). */
export const DEV_SERVER_URL = 'http://localhost:5173'
