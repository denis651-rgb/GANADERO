package bo.com.ganadero.alertas.domain;import bo.com.ganadero.alertas.application.TipoAlerta;import java.time.Instant;import java.util.*;
public interface AlertaRepository{Alerta programar(Alerta alerta);Optional<Alerta> evolucionarOrigen(Alerta alerta,Collection<TipoAlerta> tiposAnteriores);Optional<Alerta> buscar(UUID id,UUID empresa);List<Alerta> listar(UUID empresa,EstadoAlerta estado,TipoAlerta tipo,UUID animal);void resolverOrigen(UUID empresa,String origen,UUID origenId,UUID actor);void cancelarOrigen(UUID empresa,String origen,UUID origenId,String motivo);Alerta atender(UUID id,UUID empresa,UUID actor);Alerta resolver(UUID id,UUID empresa,UUID actor);int activarVencidas(Instant ahora,int limite);List<Alerta> listarPendientes(Instant ahora,int limite);void marcarEnviada(UUID id);void marcarError(UUID id,String error);}
