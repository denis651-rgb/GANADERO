# Seeds manuales

Directorio reservado para scripts de datos de prueba que no ejecuta Flyway. El seed local
de la app de escritorio (una finca, un usuario) vive dentro de la migración baseline en
`backend/src/main/resources/db/migration`, no aquí.
